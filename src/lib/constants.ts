/**
 * Default timezone for the app when user preference is not set
 */
export const DEFAULT_TIMEZONE = 'America/Los_Angeles'

/**
 * The start date for journal entries (used for calculating incomplete days)
 */
export const JOURNAL_START_DATE = '2024-09-01'

/**
 * Number of entries per page in list view
 */
export const PAGE_SIZE = 10

/**
 * Default debounce delay in milliseconds
 */
export const DEFAULT_DEBOUNCE_DELAY = 300
