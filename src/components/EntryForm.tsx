// Re-export from the refactored location for backwards compatibility
export { EntryForm as default } from './entry-form'
export type { EntryFormProps, Entry, TagType, CalendarEvent, FormState } from './entry-form'
